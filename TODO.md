# TODO — Keep only one homepage (Option A)

- [x] Copy `notepad.html` → `index.html` (overwrite) so the advanced version becomes the default homepage
- [x] Add a "GitHub Pages" comment into the new `index.html` so the smoke-test assertion passes
- [x] Delete `notepad.html`
- [x] Run `node tests/github-pages-smoke-test.js` to verify

