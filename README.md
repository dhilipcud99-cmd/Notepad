# Notepad

This repository is designed to work as a static site on GitHub Pages for note-taking and browser-based preview features.

## GitHub Pages note

The hosted GitHub Pages site can serve the frontend, but server-side execution for compiled languages requires a Node backend. The app will show a clear message when that backend is unavailable.

## Run locally

```bash
npm install
npm start
```

Then open http://localhost:3000/.
